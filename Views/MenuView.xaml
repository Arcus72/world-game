<UserControl x:Class="World_Game.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:World_Game.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.ColumnSpan="3" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            
            <Image Source="pack://application:,,,/Images/logo.png" 
               Grid.Column="1"
               HorizontalAlignment="Center"/>
            
        </Grid>

        


        <Button Grid.Column="1" Grid.Row="2"
                x:Name="Graj" Content="Start Game" 
                Foreground="Black" Background="LawnGreen"
                Margin="5" FontSize="18"
                Command="{Binding ShowGameCommand}"/>

        <Button Grid.Column="1" Grid.Row="3"
                x:Name="UserTable" Content="Scoreboard" 
                Foreground="Black" Background="BlueViolet"
                Margin="5" FontSize="18"
                Command="{Binding ShowScoresCommand}"/>

        <Button Grid.Column="1" Grid.Row="4"
                x:Name="Exit" Content="Exit" 
                Foreground="Black" Background="IndianRed"
                Margin="5" FontSize="18"
                Command="{Binding ExitCommand}"/>
    </Grid>
</UserControl>
