<UserControl x:Class="World_Game.Views.GameSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:World_Game.Views"
             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <!-- Wynik -->
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
            <!-- Ostateczna wersja zgadywania -->
            <RowDefinition Height="3*"/>
            <!-- Pole na nick -->
            <RowDefinition Height="3*"/>
            <!-- Przyciski -->
        </Grid.RowDefinitions>

        <!-- Wynik gry -->
        <StackPanel Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="20" Width="500" Height="170">


            <TextBlock Text="Game Summary"
               FontSize="45"
               Foreground="White"
               FontWeight="Bold" TextAlignment="Center"
               >
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2"/>
                </TextBlock.Effect>
            </TextBlock>


            <TextBlock FontSize="30" Foreground="LawnGreen" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2"/>
                </TextBlock.Effect><Run Text="Attempts: "/><Run Text=" "/><Run Text="{Binding AttemptsCount}"/></TextBlock>

            <TextBlock FontSize="30" Foreground="LawnGreen" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2"/>
                </TextBlock.Effect><Run Text="Time: "/><Run Text=" "/><Run Text="{Binding TimePlayed}"/></TextBlock>

        </StackPanel>

        <StackPanel Grid.Row="1">

            <Border BorderBrush="Black" BorderThickness="0,0,0,1" Padding="5" Background="DimGray" Margin="10,10,10,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Country" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="1" Text="Population" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="2" Text="Area" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="3" Text="Continent" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="4" Text="Gov." FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="5" Text="Region" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="6" Text="Year" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                    <TextBlock Grid.Column="7" Text="Language" FontWeight="Bold" Foreground="White" TextAlignment="Center" FontSize="10"/>
                </Grid>


            </Border>


        </StackPanel>
        <!-- Ostateczny wynik (wszystko na zielono) -->
        <views:CountryGuessControl Margin="10,0,10,0" Grid.Row="2"
                           DataContext="{Binding CorrectCountry}"/>

        <!-- Pole na wpisanie nicku -->
        <StackPanel Grid.Row="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Margin="10" Width="400">

            <TextBlock Text="Enter your name:"
               FontSize="30"
               Foreground="White"
               TextAlignment="Center"
               HorizontalAlignment="Center"
               Margin="0,0,0,10">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2"/>
                </TextBlock.Effect>
            </TextBlock>

            <Border Width="250"
            Height="35"
            Background="LightGray"
            CornerRadius="8"
            Padding="4"
            HorizontalAlignment="Center">
                <TextBox Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center"
                 BorderThickness="0"
                 Background="Transparent"
                 FontSize="15"/>
            </Border>
        </StackPanel>


        <!-- Przyciski -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Back to Menu" Command="{Binding GoToMenuCommand}" Width="180" Height="70" Background="BlueViolet" Margin="10" FontSize="15"/>
            <Button Content="Save Score" Command="{Binding GoToScoresCommand}" Width="180" Height="70" Background="#90EE90" Margin="10" FontSize="15"/>
        </StackPanel>
    </Grid>
</UserControl>
